// AuthService intentionally removed from this branch to avoid firebase_auth dependency
// If you want to enable Firebase Auth client integration, add `firebase_auth` to
// `pubspec.yaml` and reintroduce a lightweight token helper that signs in and
// returns `FirebaseAuth.instance.currentUser?.getIdToken()`.

