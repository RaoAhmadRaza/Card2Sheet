```bash
# copy this file to .env and fill values
# Gemini API key (server-side use recommended) - leave empty if using proxy
GEMINI_API_KEY=

# Optional: use a server-side proxy for Gemini (recommended for production)
# Set to 'true' to call your proxy instead of direct Gemini calls from the app
USE_PROXY=false
# Example: https://my-proxy.example.com  (no trailing slash)
PROXY_URL=

# Optional: override default Gemini-compatible host (for advanced setups)
GEMINI_API_HOST=

# Firebase (optional) - often handled by GoogleService-Info.plist / google-services.json
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
# Copy this file to .env and fill values.

# AI configuration
# If true, the app will call your proxy instead of direct Gemini.
USE_PROXY=false
# Example: https://my-proxy.example.com (no trailing slash)
PROXY_URL=

# Direct Gemini API key (used when USE_PROXY=false and this is set)
GEMINI_API_KEY=
# Optional: override Gemini-compatible host (advanced setups)
GEMINI_API_HOST=

# Firebase (optional) - often configured via platform files
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
